# ================================================================================
# Docker Ignore File — TASTY Training Image
# ================================================================================
# Keeps the build context small and fast.
# Checkpoint models and dataset are mounted as volumes at runtime — not baked in.

# Git
.git
.gitignore

# Node.js (not needed for training)
node_modules/
.next/
app/
components/
store/
types/
public/
pages/
prisma/
*.config.ts
*.config.js
package.json
package-lock.json
pnpm-lock.yaml

# Python virtual environments (rebuilt fresh inside the container)
.venv/
.venv-inference/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Environment files (pass as -e flags or docker run env vars, never bake in)
.env
.env.local
.env.*.local

# Large model files — mount checkpoint/ as a volume instead
checkpoint/

# Dataset images and CSVs — mount dataset/ as a volume instead
dataset/

# Uploaded media (not needed for training)
public/uploads/

# Log files
*.log
log/

# Temporary files
tmp/
temp/
*.tmp

# macOS
.DS_Store
*.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Documentation (not needed in the image)
*.md
*.txt
!requirements*.txt

# Other Dockerfiles (don't nest them inside each other)
Dockerfile*
docker-compose*.yml
